slug: "domains_prod"
subject_type: "InvDomain"
conditions:
  all:
    - has_label: { key: "env", value: "prod" }
    - domain_under_owned_domain: true
exceptions:
  any:
    - has_label: { key: "no_scan", value: true }
